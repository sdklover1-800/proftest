map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    server_name proftest.app www.proftest.app;
    return 301 https://proftest.app$request_uri;
}

server {
    listen 443 ssl http2;
    server_name www.proftest.app;

    ssl_certificate /etc/letsencrypt/live/proftest.app/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/proftest.app/privkey.pem;
    return 301 https://proftest.app$request_uri;
}

server {
    listen 443 ssl http2;
    server_name proftest.app;

    ssl_certificate /etc/letsencrypt/live/proftest.app/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/proftest.app/privkey.pem;

    root /var/www/proftest/frontend/dist;
    index index.html;

    # Backend API
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    # Health endpoint passthrough
    location = /healthz {
        proxy_pass http://127.0.0.1:8000/healthz;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA fallback
    location / {
        try_files $uri /index.html;
    }
}
