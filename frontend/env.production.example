# Optional: if frontend and backend are on the same domain (recommended),
# keep this empty and client.ts will use same-origin.
# VITE_API_URL=

# If API is on subdomain, set explicitly:
# VITE_API_URL=https://api.proftest.app
